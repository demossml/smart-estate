# План редизайна SmartEstate — Полный рефакторинг

> **Цель:** Перевести приложение на новый дизайн (тёмная тема, золотые/зелёные акценты, Cormorant SC заголовки, JetBrains Mono метрики, glassmorphism карточки) с точным соответствием дизайн-референсу `design/SmartEstate_design.md` и коду `design/SmartEstateApp_complete.tsx`.

---

## 📋 1. ДИЗАЙН-СИСТЕМА (CSS переменные / токены)

### Что сделать
Создать единый файл CSS-переменных и подключить Google Fonts.

### Где живёт сейчас
- Стили в `client-app/src/index.css` (существующая тема)
- `design/SmartEstateApp_complete.tsx` — CSS-in-JS (целевой дизайн)

### Что изменить
**Создать/обновить файл:** `client-app/src/styles/tokens.css`

```css
:root {
  --bg: #0A0C0B;
  --glass-bg: rgba(20,26,22,0.6);
  --glass-border: rgba(201,162,75,0.14);
  --gold: #C9A24B;
  --green-live: #5CC98A;
  --green-dim: #4F8F68;
  --amber: #7A5C2E;
  --danger: #B23B34;
  --text-primary: #E9E4D8;
  --text-dim: #8B9088;
  --text-muted: #5A5F58;
}
```

**Подключить шрифты** в `index.html`:
- `Cormorant SC` (500, 600, 700) — заголовки
- `Cormorant Garamond` (500, 600, italic) — акценты
- `Inter` (400, 500, 600) — тело
- `JetBrains Mono` (400, 500) — метрики

**Обновить tailwind config** — добавить новые цвета/шрифты.

---

## 📋 2. СТРУКТУРА НАВИГАЦИИ (Bottom Navigation)

### Что сделать
Заменить текущую навигацию на 3 вкладки с bottom nav:
- 🏠 **Дом** — комнаты с устройствами
- 🔄 **Сценарии** — IF-THEN правила
- ⚡ **Энергия** — энергопотребление

### Текущее состояние
- `Dashboard.tsx` — вся страница дашборда (очень много кода, смесь логики и UI)
- Есть `TabBar.tsx` для навигации

### Что изменить
1. **Переписать `Dashboard.tsx`** — разбить на вкладки
2. **Создать `HomeTab.tsx`** — список комнат + summary row
3. **Создать `ScenariosTab.tsx`** — список IF-THEN сценариев
4. **Создать `EnergyTab.tsx`** — энергопотребление + график
5. **Создать `AppShell.tsx`** — обёртка с bottom navigation (на основе `TabBar.tsx`)

---

## 📋 3. HOME TAB — Карточки комнат

### Что сделать
Переписать `RoomTileV2.tsx` в соответствии с дизайном.

### Текущее состояние
- `RoomTileV2.tsx` — существующая карточка
- Комнаты загружаются из `GET /api/rooms` через `api.getDashboardV2()`

### Целевой дизайн

#### Компактный режим
```
┌──────────────────────────────────────────────────────────────┐
│  [иконка]  Название комнаты           ⚠️ открыто        ▼    │
│            3 устройств · 21.5°                                │
└──────────────────────────────────────────────────────────────┘
```

- Иконка комнаты (Sofa/Bed/Kitchen и т.д.) — золотая
- Если в комнате есть присутствие — зелёное свечение
- Alert pill — если окно открыто или протечка (красный, `rgba(178,59,52,0.14)`)
- Шеврон золотой с rotate анимацией

#### Развёрнутый режим
- Grid 2 колонки из устройств
- Кнопка "Добавить устройство в [комната]"

#### Бэкенд
- Эндпоинт `GET /api/rooms` **уже отдаёт** комнаты с устройствами и телеметрией (строка 598 api.ts)
- Нужно доработать: добавить `friendly_name` (сейчас `friendlyName`), добавить `icon` (сейчас `icon` есть в комнатах)
- Нужно добавить `last_presence_minutes` для presence_sensor (уже есть в `lastPresenceAt`)
- Нужно доработать `computed_status` — сейчас не отображается из-за partial данных

---

## 📋 4. DEVICE TILE — Микрокарточки устройств

### Что сделать
Создать компонент `DeviceTile.tsx` (2 колонки в grid, каждый тип — своё отображение).

### Целевой дизайн (из `SmartEstateApp_complete.tsx`)

| Тип устройства | Отображение в карточке | Интерактив |
|---------------|----------------------|:----------:|
| window_sensor | Бейдж "Открыто" (красный) / "Закрыто" (зелёный) | — |
| door_sensor | Бейдж "Открыто" / "Закрыто" | — |
| presence_sensor | Бейдж "Есть" (зелёный) / "Нет · 12 мин" | — |
| motion_sensor | Бейдж "Есть" / "Нет · 12 мин" | — |
| leak_sensor | Бейдж "Протечка!" (красный) / "Сухо" (зелёный) | — |
| air_monitor | Grid 4: `22°` `44%` `620` `110` с лейблами | — |
| light | Ползунок brightness + % | ✅ Switch + Slider |
| plug | Grid 3: `120 Вт` `2.1 кВт·ч` `1.4 А` | ✅ Switch |
| gate_controller | Бейдж "Открыты" / "Закрыты" | ✅ Switch |
| climate | − `22°` + "работает" | ✅ Switch + Temp +/- |

**Футер каждой карточки:**
- 🔋 Батарея (красный если <=10%, жёлтый если <=20%)
- 📶 Сигнал (linkquality)

### Текущее состояние
- `RoomTileV2.tsx` содержит `DeviceToggleCard` и `SensorMini` — упрощённые версии
- Api отдаёт через `GET /api/rooms`: `devices` массив с `latest_telemetry`, `computed_status`, `battery`, `linkquality`

### Что изменить
1. **Создать `DeviceTile.tsx`** — новый компонент, точно как в дизайне
2. **Удалить `DeviceToggleCard` и `SensorMini`** из `RoomTileV2.tsx`
3. **Обновить типы** в `types.ts` — добавить все поля
4. **Бэкенд:** проверить что отдаются все нужные поля (`computed_status`, `battery`, `linkquality`, `last_presence_minutes`)

---

## 📋 5. SCENARIOS TAB — Управление сценариями

### Что сделать
Создать `ScenariosTab.tsx` — список IF-THEN правил с переключателями и CRUD.

### Целевой дизайн
```
СЦЕНАРИИ
IF-THEN правила движка автоматизации

┌──────────────────────────────────────────────────────────────┐
│  [ON] ЕСЛИ Влажность > 70% → Включить вентилятор         🗑  │
├──────────────────────────────────────────────────────────────┤
│  [OFF] ЕСЛИ Никого нет 15 мин → Выключить всё            🗑  │
└──────────────────────────────────────────────────────────────┘

[Условие: ______________________________]
[Действие: ____________________________]
[➕ Добавить сценарий]
```

### Текущее состояние
- `GET /api/scenarios` — возвращает список сценариев (строка 908 api.ts)
- `POST /api/scenarios` — создать
- `POST /api/scenarios/:id/toggle` — включить/выключить
- `DELETE /api/scenarios/:id` — удалить

### Что изменить
1. **Создать `ScenariosTab.tsx`** — новый компонент
2. **Подключить API** сценариев
3. Переключатели (ON/OFF) — сохранять в БД

---

## 📋 6. ENERGY TAB — Энергопотребление

### Что сделать
Создать `EnergyTab.tsx` с hero-цифрой, гистограммой 24ч и списком розеток.

### Целевой дизайн
```
ЭНЕРГОПОТРЕБЛЕНИЕ
Розетки и общий расход дома

      0.34
    кВт сейчас

[гистограмма 24ч — золотой градиент]

[🔌] Телевизор   120 Вт
[🔌] Чайник        0 Вт
```

### Текущее состояние
- `GET /api/energy` — есть (строка 734 api.ts)
- Гистограмма — нужно добавить эндпоинт для тренда за 24ч

### Что изменить
1. **Создать `EnergyTab.tsx`** — новый компонент
2. **Добавить бэкенд эндпоинт** для получения энерготренда за 24ч (почасовые срезы мощности)
3. Список розеток — данные из `devices` с `type='plug'`

---

## 📋 7. МОДАЛЬНЫЕ ОКНА

### Что сделать
Создать модальные окна по дизайну:
- **AddDeviceModal** — 2 шага (выбор типа → имя + комната)
- **AddRoomModal** — название + иконка

### Текущее состояние
- `RoomAddModal.tsx` — существует, но отличается от дизайна
- `Dashboard.tsx` содержит `handleCreateRoom`

### Что изменить
1. **Переписать `RoomAddModal.tsx`** — новый дизайн (bottom sheet стиль)
2. **Создать `AddDeviceModal.tsx`** — двухшаговый (тип → настройка)
3. **Бэкенд:** `POST /api/devices` — создать устройство (строка 293 api.ts — уже есть, но нужно доработать)

---

## 📋 8. БЭКЕНД — Доработки

### API эндпоинты (существующие, нужно проверить)

| Эндпоинт | Статус | Что нужно |
|---------|:-----:|-----------|
| `GET /api/rooms` | ✅ Есть | Добавить `icon` в ответ, `friendly_name`, `last_presence_minutes` |
| `GET /api/scenarios` | ✅ Есть | Вернулся только `id, condition, action, active` — норм |
| `POST /api/scenarios/:id/toggle` | ✅ Есть | — |
| `DELETE /api/scenarios/:id` | ✅ Есть | — |
| `GET /api/energy` | ✅ Есть | Добавить почасовой тренд |
| `POST /api/devices` | ✅ Есть | Нужно доработать под новую схему |
| `POST /api/rooms` | ✅ Есть | — |
| `GET /api/devices` | ⚠️ Неполный | Проблема с `friendlyName=null`, `computed_status=null` |

### Критические баги
1. **`friendlyName=null`** в `/api/devices` — нужно разобраться почему база отдаёт null при перезапуске
2. **`computed_status=null`** — проблема в том, что `computeDeviceStatus` вызывается в `/api/rooms` запросе, но devices без телеметрии в памяти не получают статус
3. **`lastPresenceAt`** — in-memory Map, сбрасывается при рестарте. Нужно инициализировать из БД при старте

### Новые эндпоинты
1. **`GET /api/energy/trend`** — почасовые срезы энергопотребления за 24ч
2. **`POST /api/devices/:id/settings`** — отправка MQTT команд для настройки датчика (чувствительность, тайм-аут)

---

## 📋 9. ПОРЯДОК ВЫПОЛНЕНИЯ

### Фаза 1: Дизайн-система + базовая структура
```
1.1  Создать tokens.css — CSS переменные
1.2  Подключить Google Fonts
1.3  Обновить tailwind.config
1.4  Создать AppShell.tsx — bottom navigation
1.5  Создать HomeTab.tsx — обёртка списка комнат
1.6  Создать ScenariosTab.tsx — заглушка
1.7  Создать EnergyTab.tsx — заглушка
1.8  Переписать Dashboard.tsx — использовать AppShell
```

### Фаза 2: Карточки комнат и устройств
```
2.1  Переписать RoomTileV2.tsx → новый дизайн
2.2  Создать DeviceTile.tsx — все типы устройств
2.3  Удалить старые DeviceToggleCard / SensorMini
2.4  Подключить live обновления (WebSocket)
2.5  Починить computed_status и friendlyName
```

### Фаза 3: Сценарии
```
3.1  Создать ScenariosTab.tsx — список с CRUD
3.2  Подключить API сценариев
3.3  Тестирование
```

### Фаза 4: Энергия
```
4.1  Добавить бэкенд эндпоинт energy/trend
4.2  Создать EnergyTab.tsx
4.3  Подключить API
4.4  Тестирование
```

### Фаза 5: Модальные окна
```
5.1  Переписать RoomAddModal.tsx
5.2  Создать AddDeviceModal.tsx
5.3  Доработать бэкенд для создания устройств
5.4  Тестирование
```

### Фаза 6: Бэкенд баги
```
6.1  Починить friendlyName в /api/devices
6.2  Починить computed_status
6.3  Инициализировать lastPresenceAt из БД при старте
```

---

## 📋 10. РИСКИ

| Риск | Описание | Решение |
|------|----------|---------|
| **friendlyName=null** | При перезапуске сервера devices теряют friendly_name | Разобраться с запросом в `/api/devices` — скорее всего проблема в JOIN или в том, что `devices` таблица пуста при старте |
| **computed_status не работает** | Зависит от `latest_telemetry` в памяти | Нужно пересчитывать на основе последней записи из БД, а не только in-memory |
| **lastPresenceAt** | Сбрасывается при рестарте | Сохранять в БД или инициализировать при старте запросом `SELECT MAX(ts) FROM telemetry WHERE property='presence' AND value=1 GROUP BY device_ieee` |
| **WebSocket live** | Буфер в `Dashboard.tsx` использует `friendly_name` для поиска устройства | Проверить что friendly_name совпадает с тем, что приходит от Zigbee2MQTT |
| **DuckDB lock** | Невозможно читать БД, если сервер запущен | Использовать `read_only=True` или DuckDB connection pooling |
