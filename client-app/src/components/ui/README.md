# components/ui/ — заготовка миграции на react-router-dom

**НЕ ПОДКЛЮЧЕНО к main.tsx.** Часть той же миграции, что `pages/*`.

Содержит дублирующиеся по имени файлы с top-level `components/*` (DeviceTile, AddDeviceModal, EnergyTab, ScenariosTab).

См. `pages/README.md` для контекста.
